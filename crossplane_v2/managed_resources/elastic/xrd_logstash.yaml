apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: xlogstashinstances.koncept.bluesolution.es
  annotations:
    koncept.io/tier: "2"
    koncept.io/operator: "eck"
    koncept.io/operator-version: "2.x"
    koncept.io/last-reviewed: "2026-06-04"
spec:
  scope: Cluster
  group: koncept.bluesolution.es
  names:
    kind: XLogstashInstance
    plural: xlogstashinstances
  claimNames:
    kind: LogstashInstance
    plural: logstashinstances
  defaultCompositionRef:
    name: xlogstashinstance-eck-composition
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      additionalPrinterColumns:
        - name: NAMESPACE
          type: string
          jsonPath: .spec.namespace
        - name: VERSION
          type: string
          jsonPath: .spec.version
        - name: REPLICAS
          type: integer
          jsonPath: .spec.replicas
        - name: READY
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
        - name: AGE
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              description: Logstash instance configures Logstash log processing via ECK operator.
              required:
                - namespace
                - version
              properties:
                namespace:
                  type: string
                  description: Kubernetes namespace where Logstash runs.
                clusterName:
                  type: string
                  description: Name of the Logstash instance (Claim name if unset).
                  default: ""
                version:
                  type: string
                  description: "Logstash version (must match Elasticsearch version)."
                  pattern: "^[0-9]+\\.[0-9]+(\\.[0-9]+)?$"
                replicas:
                  type: integer
                  description: "Number of Logstash replicas."
                  default: 1
                  minimum: 1
                  maximum: 5
                elasticsearchRef:
                  type: string
                  description: "Reference to Elasticsearch cluster name in same namespace."
                beatsPort:
                  type: integer
                  description: "Beats input port."
                  default: 5000
                  minimum: 1
                  maximum: 65535
                pipelineConfig:
                  type: string
                  description: "Logstash pipeline configuration."
                  default: ""
                resourceCpuRequest:
                  type: string
                  description: CPU request.
                  default: "250m"
                resourceMemoryRequest:
                  type: string
                  description: Memory request.
                  default: "512Mi"
                resourceCpuLimit:
                  type: string
                  description: CPU limit.
                  default: "1"
                resourceMemoryLimit:
                  type: string
                  description: Memory limit.
                  default: "2Gi"
                environment:
                  type: string
                  description: "Target environment tier: local, development, staging, production."
                  default: production
                  enum:
                    - local
                    - development
                    - staging
                    - production
                owner:
                  type: string
                  description: "Team or system that owns this instance."
                  default: ""
            status:
              type: object
              properties:
                conditions:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      reason:
                        type: string
                      message:
                        type: string
                      lastTransitionTime:
                        type: string

